# Angus Shield handover (temporary)

These are the starter files for the private `angus-shield` repo, parked here only because this
session could not reach that repo. Next session:

1. Push everything in this folder (except this file) to `angus-shield`, with the same paths.
2. Create the Vercel project `angus-shield` under the SPEC Business Solutions team, linked to the
   repo, and connect `angusshield.com` as the main domain.
3. Delete this folder, and replace the two Angus Shield entries in `DECISIONS.md` with a one-line
   pointer to the `angus-shield` repo.
