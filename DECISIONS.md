# Decisions log

Record every correction or choice made while building. Newest at the bottom.

- 2026-09-06 — Company documents are about SPEC Business Solutions, not the founder personally. Personal goals stay out of this repo.
- 2026-09-06 — Role first, person second: criteria hang off roles; `role_assignments` keeps placement history separately.
- 2026-09-06 — Claude is the interface (guided onboarding, explanations, board output), not a bolt-on feature.
- 2026-09-06 — Scores are computed, never stored. Periods are append-only and lockable.
- 2026-09-06 — Source workbook pillar weights do not always sum to 100% (e.g. 85%, 91%). Engine normalises by applicable weight so it reproduces the workbook; the UI must enforce 100% on save.
- 2026-09-06 — Integrations (job system, accounting, CRM, safety) come after manual/CSV entry works. Phase 4.
- 2026-09-06 — Drizzle ORM instead of Prisma: Prisma needs downloaded query engines, which the build environment could not fetch. Drizzle is pure TypeScript, supports SQLite (dev) and Postgres (prod), and is easier for Claude Code to reason about.
- 2026-09-06 — Team averages are computed over scored roles only. An unscored role is missing data, not a zero; the executive summary says how many roles are scored.
- 2026-09-06 — Local dev needs no accounts: `npm run db:push && npm run db:seed && npm run dev` gives a working demo tenant ("Acme Electrical") on SQLite.
- 2026-09-06 — The four questions are the front door (`/start`); the answers are the tenant's first diagnostic and steer which pillars' KPIs are emphasised.
- 2026-09-06 — Claude registration is a hard gate: nothing in the journey opens until the business confirms a Claude workspace with seats for supervisor level and above.
- 2026-09-06 — Journey step status is computed from data, never ticked by hand.
- 2026-09-06 — Two tiers: Basic (self-serve) and Program (consulting arm on site). Sign-up lands on Basic; Program is requested from the journey page.
- 2026-09-06 — Board output is generated on period lock from the data; Claude rewrites it in plain terms when an API key is present, otherwise the deterministic version stands. Never presents an empty template as a result.
- 2026-09-10 — Scoring engine brought to BUILD_SPEC §3. A blank is Pending, so NA; NA is excluded from both sides of the pillar fraction. A pillar with nothing scorable has no score (null), not 0, and is left out of the role mean. Team % is the mean of role % across scored people. No rounding mid-calculation. Supersedes the 2026-09-06 workbook rule that kept blanks in the denominator; the workbook cases still reproduce because every KPI in them is Y or N.
- 2026-09-10 — Watch scores NA (was N): started, not finished. The guard is `resolveWatchAtLock` — Watch two closed months running becomes Not met on the second lock. Not yet wired into `lockPeriod`; that belongs with the period_scores work (§1.2, §5).
- 2026-09-10 — Locked months keep the answers they were locked with (Watch stored as N) — nothing recalculates history. Open months score from the status. Blanks in an already-locked month now read as NA rather than 0; until period_scores freezes scores at lock, that is the one way the engine change reaches a closed month.
- 2026-09-10 — Access levels: `administrator` · `full` · `readonly`, stored as `readonly` (BUILD_SPEC says `read_only` — the spec should catch up). An administrator manages within scope exactly as `full` does: every management check is `canManage`, never `=== 'full'`, which would lock the GM (the first administrator) out of their own business. Seats and invites still accept `full` too — tightening them to administrator-only is the seats rework (§8.3).
- 2026-09-10 — Sign-in links failed with `otp_expired` ("Email link is invalid or has expired") even when clicked straight away: email security scanners open links to check them, spending a one-time link before the person does. The sign-in email now lands on `/auth/confirm`, a page with one button; the token is spent only when it is pressed. Works on any device, because the token is in the link rather than in the requesting browser's cookie. `next` after sign-in is only ever a path inside SPEC.
- 2026-09-10 — SPEC now sends the sign-in email itself, so no Supabase template change is needed: the service-role client asks Supabase for a token only (`generateLink`, which sends nothing — `invite` when the person has no sign-in identity yet), and Resend sends one email linking to `/auth/confirm`. Without the service-role key or Resend it falls back to Supabase's own email. The sign-in send throws on failure rather than showing "check your email" for an email that never went. Not yet rate-limited by SPEC — Supabase's send limit no longer applies on this path.
- 2026-09-10 — Removed `/admin` "sign in as", which let anyone on ADMIN_EMAILS sign in to any customer's business. BUILD_SPEC §9.1: SPEC as a company has no access and there is no break-glass. The action file is deleted, not just unlinked — a server action is a public endpoint whether or not a button points at it. `/admin` still lists each business's name, plan and GM contact (account data, needed for billing and the Program); its journey-progress column is derived from customer content and is for the founder to decide on.
- 2026-09-10 — Bot protection at sign-up, none of which asks a person to do anything: a trap field people never see; a signed timestamp (a form posted in under 3 seconds, older than 2 hours, or forged is refused); at most 3 new businesses per network address per hour; and Cloudflare Turnstile, which switches on when `NEXT_PUBLIC_TURNSTILE_SITE_KEY` and `TURNSTILE_SECRET_KEY` are set. If Cloudflare is unreachable the person is let through and it is logged — a real business is never locked out because a third party is down. Sign-up errors are now messages on the form, keeping the four answers.
- 2026-09-10 — The one-per-minute sign-in email limit is also kept on the person's Supabase sign-in identity (`app_metadata.spec_link_sent_at`), so it holds across servers. Checked before a new token is made, because making one cancels the link already in their inbox.
- 2026-09-10 — "Your board pack is ready" carries no link and no content, not even the business name or the month (§10). `/admin` now shows accounts only — name, plan, start date, administrator, Program requests — and nothing derived from what a customer put into SPEC; the contact comes from the administrator's seat, not the chart. Vendor names removed from the scorecard hint and the schema.
- 2026-09-10 — Incentive rebuilt to §6: seven levels ($4,000 halving to $250; the 6 September `staff` level paid as technician until re-levelled), director outside the scheme; a failure is a pillar under 50% anywhere beneath them (was: a majority of KPIs missed, direct reports only); Sales Ace doubles the ceiling, earned by three consecutive months of both halves, lost by two consecutive failures, never backdated; first period without incentives; a merge pays the higher ceiling once on the mean of the two roles. Pure logic — not yet shown on any screen.
- 2026-09-10 — Regional prices (§8.1) live in `src/lib/pricing.ts`, tested to reduce to 8. Checkout charges the business in its own currency, chosen from the country the hosting platform detects, and picks the Stripe price on the seat product that matches the published amount exactly — a price that has drifted in Stripe is never charged; if none matches it falls back to the configured AUD price and logs it. The price a leader reads in the app comes from the same signal, so it matches what they are charged. Until `tenants.region` exists (§1.1) the detected country is the stand-in for "where the business is". The six regional prices already exist in Stripe test mode; live mode has none yet.
- 2026-09-10 — One email can hold more than one business (a group owner, a consultant, a founder with a sandbox) — the schema always allowed it; sign-in now honours it. Signing in links every seat held under the verified address; the business shown is the one last chosen on that browser (`spec_business` cookie, only ever a business the person has a seat in), else the first joined. "Switch business" appears only to someone with more than one. Signing up with an address already on SPEC now sends the sign-in link straight away instead of a second form asking for the same address.
- 2026-09-10 — The front door is delightfully easy, and the questions come later. Welcome is one line and one button; sign-up is name, business and email; a brand-new business goes straight in with no email step, landing on its own chart. The four questions and the drill-down are asked inside, once, not before entry. Supersedes the entry sequence on screen 1 (The Welcome) — the founder's instruction, 10 September.
- 2026-09-10 — Entering without an email proves nothing about the address, so an address is PROVEN only by signing in through a link sent to it (`app_metadata.spec_email_proven`, set by /auth/confirm and /auth/callback). Seats are linked by email only for a proven address; a sign-up links only the seat it just created. Without this, someone who signed up using another person's address would inherit every seat later given to it. An address already on SPEC always gets a link, never entry.
- 2026-09-10 — Email and password, not sign-in links: "stop sending the link — I will lose everyone like this." Sign-up is name, business, email and password, straight in, no email; sign-in is email and password. Email is sent for exactly two things: "Forgot password?" (a set-password link) and confirming the address the first time someone adds a seat. Security arrives when it matters — adding seats and connecting systems — never at the door. Sign-in attempts are limited to ten per address per network per fifteen minutes. Supersedes the magic-link sign-in of 6–10 September.
- 2026-09-10 — Production functions moved from iad1 (US East) to syd1 (Sydney), beside the database: every query had been crossing the Pacific, and sign-up took 30 seconds. Setting up a business now writes each role's KPIs in one batch instead of one row at a time. Buttons grey out while working, so nothing is submitted twice.
- 2026-09-10 — Security pass before opening sign-ups: every server action that takes an id from a form now checks it belongs to the caller's business (gates, board approval, role change, scorecard KPIs); KPI setup obeys scope; billing (checkout, portal) and the board pack need an administrator or the top of the chart. The app connects with a database role that bypasses RLS, so these application checks are the isolation — RLS is defence in depth only.
- 2026-09-11 — SPEC Today (`/today`) is the page everybody starts the work day on, and sign-in lands there; someone holding no role is passed on to the journey. Every block is derived from what the business recorded — pillar lights, what needs you, direct reports, connected feeds, changes, training — with the scope resolved once at the top so nothing on the page can reach a role the viewer is not entitled to see. The Ask panel computes its answers from the person's own card: no model call, no key, no write, per "read to inform, never write to change". Messages links out to the person's own mail and holds no content. The one write is the weekly meeting — a date, never what was said, and logging twice in a week is a no-op.
- 2026-09-11 — The whole app moves onto the Organic design system: tailwind.config.ts is the token sheet (cream ground, sand surfaces, terracotta accent, sage second accent, 100–900 ramps), Caprasimo over Figtree through next/font, containers over-rounded and controls pilled. The four pillar colours were retuned onto the same warm axis — terracotta, sage, ochre, mulberry — because the old blue (#169BD5) and violet (#8064A2) read as borrowed on a cream page. A separate `light` scale carries the traffic lights, and its pending step is a warm neutral: "pending is never red" is now enforced by the palette rather than by remembering.
- 2026-09-11 — Training is modelled properly rather than borrowed from the Compliance pillar, which was showing governance KPIs ("Monthly board output delivered on time") as though they were somebody's training. Three tables: `training_modules` (SPEC's own catalogue, per tenant), `role_curriculum` (which modules a role requires) and `training_records` (one person's progress through one module). The path belongs to the ROLE — role first, person second, so a role may require training with nobody in it and reassigning somebody never edits it. Progress belongs to the PERSON, because a person learns a module once. Sign-off belongs to the PLACEMENT (`role_assignments.trained_at` / `trained_by`), so it does not travel with somebody who moves to a different job. Sign-off stays a manager's decision: the bar reaching the end makes a person *ready* to be signed off, never signed off. Seat of the catalogue is `seed/training_modules.json`, installed at provision with a starting path per role level — SPEC proposes, the leader edits. Overdue training reaches "what needs me today" because it reaches the board pack regardless of scores.
- 2026-09-11 — Ace eligibility is reported as three separate steps — path complete, signed off, three closed months at 90% — rather than one verdict, so a person who has done two of them is told which one is outstanding. `lib/training.aceSteps` says where somebody stands; `lib/incentive` and BUILD_SPEC §6.4 still govern the money, and this changes neither. The later instruction ("trained on the job, minimum 90% on the KPI board three months in a row") is recorded here for the spec to catch up: §6.4's current rule is outcome plus behaviours, which is not the same test.
- 2026-09-11 — Local development runs on a Postgres on the developer's own machine, with `DATABASE_URL` in `.env.local` (git-ignored, and it takes precedence over `.env`) so `db:push` can never be aimed at the hosted database by accident. README's setup was still describing the SQLite `data/dev.db` this repo stopped using; it now describes what actually works. `npm test` needs no database at all — every rule in `src/lib` is pure.
- 2026-09-11 — Basic and Advanced, decided by one question to the leader ("do you want the power of AI?"), stored on `tenants.tier` and defaulting to `basic`. Basic has no connectors and no assistant: every number typed in and confirmed by a name. It is a complete way to run the whole system rather than a crippled one — no feature anywhere is reachable only by connecting something — and it costs the same, because it is the same system. Nothing switches itself on for a business that has not asked for it.
- 2026-09-11 — The weekly meeting generates its own agenda from the month as it stands: a failing hard gate first, then the number that moved the wrong way, then work that is late, then a miss nobody explained, then a vacancy. Three is a ceiling and what did not fit is counted rather than dropped. `meetings` gains attendance and decisions as their own columns — a senior meeting the senior group did not attend is a note, and a decision outlives the week it was made in. The read on the week is deterministic, so the same month always produces the same record.
- 2026-09-11 — `assessment_periods.status` gains `submitted` between open and locked, with submitted_by/at and signed_by/at. Submitting says "finished as far as I am concerned"; signing says "the business accepts it"; locking follows the signature rather than a date. Submitting is not a trap — a submitted month is reopened and corrected, and only a LOCKED month is corrected by dated amendment, because only a locked month is history.
- 2026-09-11 — Monthly scoring reads provenance, not just results: fed by a connected system, confirmed by a person, waiting for a name, or not tracked at all. A fed row is read-only — hand-editing it would break the one thing that makes a fed number worth more than a typed one. A row claiming a source that is NOT connected is deliberately not treated as fed; that is the case where a number would silently stop updating with nobody noticing. Flags block on a miss with no reason and on a manual number nobody confirmed, and deliberately do NOT block on an honestly reported gap: blocking that would push people to invent numbers.
- 2026-09-11 — Approvals is one queue, and most of it is derived rather than filed — a month waiting to be signed is a period with status `submitted`, a path waiting for a manager is a finished path with no signature against it. Derived items cannot drift out of step with the business and nobody has to remember to close them. Only decisions needing a record of their own (a board approving a connector, money, a renegotiated target) are stored in `approvals`. Sorted by how long each has waited, never by importance: SPEC does not rank one person's decision above another's.
- 2026-09-11 — The org chart's layout is computed — one recursive walk assigning a centre x and a depth row, absolutely positioned cards, and three kinds of connector rectangle. Nested flex columns produced unconnected lines and a recursion bug and must not come back. Anything the walk never reaches (a cycle, a branch under a detached role) is placed as a root of its own: a chart must never render blank because one reporting line is wrong. Dragging a card moves the role and everything under it; dragging a name moves the person and swaps if the destination is filled, because silently vacating loses a placement nobody asked to lose.
- 2026-09-11 — Connectors are by category and never by vendor, which is also what makes board approval meaningful: a business cannot route around it by using a product SPEC has never heard of. Financial and people systems raise a board request carrying the exact data scope in plain words, sit disconnected until it is decided, and the server refuses to mark one live without an approval behind it. A decline is a real outcome — the system stays off and its KPIs stay manual.
- 2026-09-11 — The permission matrix in `lib/permissions` is a DESCRIPTION of what the server enforces, laid out so an administrator can read it in one go. It is never consulted before a write; hiding a button was never access control. Two permissions are undelegable at any level, including by an administrator acting for themselves: approving a sensitive connector, and signing off a period. A checklist role always sees its own card in full — being measured by something you are not allowed to read would be indefensible.
- 2026-09-11 — People answers both halves from the org chart rather than a separate HR silo. Clear to Work is pass or fail per person and reads "not established" where nothing has been recorded, because assuming clear when nobody checked is the exact failure the gate exists to prevent. The cost of a vacancy is written in pillars and orphaned roles, never in dollars — SPEC does not know what the work was worth, and a made-up figure would undermine every real number on the page. Candidates are rated against the four pillars, and an unrated pillar is left out rather than counted as zero.
- 2026-09-11 — The hiring checks, the sector measures and the training catalogue are deliberately generic. The handoff designs named one client's licences, pay bands, awards, advertising channels and software vendors; hardcoding any of those would make SPEC fit exactly one customer. What is fixed is the SHAPE — that each check has an answer, that each pillar is measured by something, that each role starts with core modules — and the specifics belong to the business. Tests assert no industry or vendor leaks back in.
- 2026-09-11 — The labour calculator claims 10–30% of the LABOUR component, never of revenue: labour is the part a business controls month to month. It shows both percentages and the revenue one is always smaller. Every line can be switched off and switching one off removes it from the headline and every figure; recovery rates are published per line rather than rolled into one number, so each is arguable. No line names a problem without naming what SPEC does about it.
- 2026-09-11 — The front door stays one line and one button (the founder's instruction of 10 September). The fuller argument lives at `/pricing` and `/sectors`, linked from it rather than folded back into it. The Landing design in the September handoff is therefore implemented as those two pages, not as a rebuilt `/welcome`.
- 2026-09-11 — `/site` is the on-site view: four targets and nothing else, on plain form posts so there is no bundle to download before the first tap works on one bar of signal. Reporting a near miss is open to anybody with a seat including readonly — one only a manager can file is one that does not get filed — and it lands as an action on this week's meeting with a name against it.
- 2026-09-11 — The shallow J curve is a named design principle and a measured feature, not a claim. Every transformation dips before it climbs, and the dip is deep because discovery is manual; SPEC's mechanism is specific — when a business's own systems feed the KPIs, the picture exists the day they connect, so the engagement starts at execution instead of at mystery-solving. `/curve` measures each business's own phases from its own records and is deliberately able to say no: a business whose discovery ran longer than the benchmark is told so, because a page that can only congratulate the product is not a measurement. The `HAND_BUILT_DISCOVERY_DAYS` benchmark is the one number that is not the business's own data and it is labelled as an assumption wherever it appears. The stretch before the first close is drawn with NO value rather than a low one — during discovery a business is not performing badly, it is invisible, and an invented dip would be the product arguing its case with a number nobody took. **The collapse is caused by connectors, so it happens on Advanced and not on Basic**, and both the product and the pricing page say so rather than letting the claim spread across the whole price list.
- 2026-09-11 — Conversation boards (`/boards`) are a distinct object from both the board of directors and a dashboard: a visual built to provoke one specific realisation from data the business already recorded, so a hard conversation has a neutral third party in it. Three rules, all enforced in code and tested. **The board never labels the person** — the first one worked because a director recognised himself in his own numbers, and a board that says "you are doing this" is the consultant confronting him again wearing a chart; it is also the rule book's instruction never to name somebody's psychology back at them. The word from the original brief is therefore in the source comments as its origin and never on screen. **A board is only ever about the person asking for it** — there is no roleId parameter and no way to pass one, because the moment it can be pointed at somebody it stops being a mirror and becomes surveillance. **Every board carries the one change that would move it**, since a realisation with no next action is just bad news about yourself. Comparison is always against a described way of working, never against another person or business — a conversation board is exactly where a leaderboard would be most tempting and most damaging. The first three: who decides here, who is actually in the room, and where things wait. The sharpest signal is targets agreed exactly as proposed, because SPEC's own claim is that a target is agreed and never imposed; if none ever moved, none was.
- 2026-09-11 — A template target is a placeholder, not a number, and `{net_profit_target}` was rendering on screen as a variable name. `lib/targets.resolveTarget` splits it the way the product actually works: the DEFAULT written into the template becomes the PROPOSED target — SPEC proposes, the leader edits — and the agreed target stays empty until somebody agrees it. A placeholder with no default proposes nothing, because inventing a number for a business SPEC knows nothing about is the one thing it must never do. `ScorecardRow` carries both, and an unagreed number reads "85% proposed, not yet agreed" rather than filling the target field, so it can never be mistaken for one the business signed up to. The field showing it is a div, never an input: nothing SPEC proposed can be saved back as if a person had agreed it.
- 2026-09-11 — Flags on `/scoring` group by role and kind for display only. A vacant role raises the same flag against all sixteen of its measures, and sixteen near-identical cards bury the one flag that is actually about somebody's work. `groupFlags` collapses them into one line that still names every measure behind a disclosure — a count on its own tells a leader they have a problem without telling them where it is — and a group of one keeps its own wording, so a single problem never reads as a statistic. `blocking` still counts row by row: a month is blocked by the number of unfinished rows, never by the number of cards on a page.
- 2026-09-11 — Linking on the J curve is measured from the day the business started, not from the day discovery ended. Connecting a system is something a business does from day one, and in practice it FINISHES first — which is the entire mechanism. Anchored to the end of discovery it reported nought days, so the product's central claim rendered as what looked like a gap in the data. Where the systems were feeding before the picture was finished the page now says exactly that, because it is the strongest true form of the claim.
- 2026-09-11 — The **Board Charter**: four commitments, one per pillar, that do not change between businesses and are not negotiated. Zero harm physical or mental; never lose great talent; gross profit at the percentage this business needs to be profitable; no breach of the commitments the business has already made. Each is carried by a HARD GATE rather than by a criterion inside a pillar's percentage — the existing Zero Harm mechanism generalised rather than a new one invented, so a commitment is pass or fail beside the score and never averaged into it. A commitment three good measures sitting next to it can dilute is not a commitment, and a business that hurt somebody has not had a 94% month. Safety deliberately reuses the `zero_harm` gate businesses already enter, because SPEC never re-asks for what it has been told. Gate keys are strings, so this needed no schema change at all.
- 2026-09-11 — The charter splits on BASIS, and that split is the whole idea. Three commitments are ABSOLUTE: their figure is nought and is never derived from what the business has been managing, because averaging a year of injuries to set an achievable target is a statement about how many people you expect to hurt. Exactly one is EARNED — gross profit — and it is earned precisely because it is the only one of the four that is a fact about this business rather than a line every business holds. SPEC cannot supply it: a margin taken from anywhere else is either comfortable enough to be useless or high enough to be ignored.
- 2026-09-11 — A target is judged SOUND or not, which is a different question from whether it was agreed. `lib/targets.soundness` reads every target against the median of the business's own last twelve closed months — the median, not the mean, so one shutdown or one enormous job cannot reset what everybody is held to. At or below the actual is TOO EASY: met by carrying on exactly as before, quietly adding a green light to every month while nothing improves. Up to a fifth above is SOUND. Beyond that is OUT OF REACH: missed every month, and a measure that is always red stops being read — after which the rest of the card is read the same way. Under six closed months SPEC declines to judge and says so, because a confident answer from evidence that does not support one is worse than no answer. `MAX_STRETCH` (20%) and `MIN_MONTHS_TO_JUDGE` (6) are published constants, arguable like the J curve's discovery benchmark.
- 2026-09-11 — **A target of nought is a commitment, not a stretch, wherever it appears.** The stretch band is relative and nothing is within a fifth of nought, so a purely relative test declared every zero target out of reach — including "zero incidents" and "zero breaches". Telling a business its zero-harm target is unrealistic would be the worst sentence this product could produce. Zero targets get the charter's exemption at role level too. The exemption is for nought itself and not for every small number: a ceiling the business is nowhere near is a real finding and is still reported as one.
- 2026-09-11 — The worked example from the brief — an electrical contractor at 40% gross profit, where 35 is too low and 50 is too high — lives in a TEST and never in the seed templates. A trade's real margin is a fact about one business, and shipping it would make SPEC fit exactly one customer. What ships is the rule that finds it. Same resolution as the sector measures and the training catalogue.
- 2026-09-11 — `/curve` gains "what is holding the dip open": discovery is the part SPEC shortens, and it is not the only thing that makes a dip deep. Measures nobody has met, measures nobody could miss, targets beyond the record, roles with nobody in them, scored roles with nothing underneath them — each with the one change that would move it. **The brief calls this change resistance and negativity, and those words must never reach the screen.** The rule book forbids naming somebody's psychology back at them, and a leader told their people are negative argues with the claim instead of fixing the cause. SPEC shows the CAUSE and lets them draw the conclusion, which is the conversation-board mechanism and the reason the first board worked. A test asserts that no word for anybody's attitude appears anywhere in the output. The list is ordered by consequence rather than by count — sorting by size would bury the sharpest finding under the largest pile — and the headline names that finding instead of totalling everything, because "34 things" reads as an indictment and tells nobody where to start.
- 2026-09-11 — The demo seed carries nine months (eight closed) rather than five, because the soundness engine will not speak under six and a demo that only ever says "not enough history" demonstrates nothing. It seeds both failure modes on one card: utilisation agreed at 85% against an actual of about 69% — an industry figure, never met, never going to be — and gross profit agreed at 35% against an actual of about 40%, met every single month without anybody doing anything differently. Results are now generated to match their own measure rather than cycled blindly across indexes: a "zero incidents" row carrying "18% against 15%" is not just untidy, because the soundness engine reads those as the rolling actual and nonsense there produces a confident, wrong verdict about a target on screen.
- 2026-09-11 — The public site carries the new design work: `/how` holds the Board Charter, how a target is earned rather than agreed, the shallow J curve and conversation boards. **Every claim on it is RENDERED from the objects the product runs on** — the charter from `lib/charter`, the stretch band and the worked example from `lib/targets`, the discovery benchmark from `lib/jcurve` — and never restated in prose. That is the point rather than a convenience: a marketing page that paraphrases the product drifts from it within a release or two, and then the site is quietly making a claim the software no longer honours. Here it cannot — change the rule and the page changes with it, or the build fails. Tests assert the charter's own sentences do not appear as literals in either page that shows them.
- 2026-09-11 — The J curve argument moves from `/pricing` to `/how`, because it is a question about the method rather than about the price. What stays on pricing is the part that genuinely IS a pricing fact, stated beside the two prices: the collapse is caused by connectors, so it happens on Advanced and not on Basic. A claim that quietly spreads across the whole price list is the overclaim that would make every other number there worth less, and a test holds that sentence in place.
- 2026-09-11 — One `PublicNav` for every page reachable without signing in. The nav had already drifted — pricing had lost "By sector" — and a public site whose navigation changes depending on which page you landed on reads as unfinished before anybody has seen the product. It is deliberately not the signed-in `Shell` header: inside, the nav is for somebody doing a job; outside, it is for somebody deciding whether to start, and the two lists have almost nothing in common.
- 2026-09-11 — The charter goes on `/sectors` as well, because that page's whole argument is that the four questions do not change and only the measures do — and the charter is the sharpest form of it: four commitments word-for-word identical in a workshop, a clinic and a construction firm. The original Sectors design already carried a `gate` line per pillar ("Zero Harm is a hard gate", "Clear to Work is a hard gate"), so the charter is the design's own intent extended to People and Earnings rather than a new idea bolted on.
- 2026-09-11 — `/welcome` stays one line and one button and gains a third link, not a section. The founder's instruction of 10 September stands, and a test now holds it: the file must stay under 45 lines and contain no `<section>`, because a welcome page is the single most tempting thing in any product to quietly grow a brochure onto.
- 2026-09-11 — `/api/health` checks the database's SHAPE, not just that it answers. A deploy can succeed, the app can start, `select 1` can pass, and the product can still be broken because the build shipped with a column the database has not been given — a 500 on some pages and silence on the others, which is the hardest failure to diagnose and the easiest to ship. The expectation is derived FROM the Drizzle definitions rather than written out by hand: a second list of columns drifts the first time somebody adds one, and a drifted check reports healthy, which is worse than no check. The comparison is deliberately one-directional — a column the database has and the code does not is what every rollback and mid-migration state looks like, so flagging it would cry wolf on exactly the deploys somebody is already watching. It reads `information_schema` only, touches no tenant row, and returns no values, so it stays safe to expose to an operator who may not look at a customer's numbers. Proven in both directions against real databases before shipping: 503 naming the missing table and column against a database one release behind, 200 against a correct one.
- 2026-09-11 — The database is brought up to the build AUTOMATICALLY, in the build step (`scripts/deploy-migrate.ts`, wired into `npm run build`). This replaces asking a person to run a database migration from a command line at the moment a release goes out — not a reasonable thing to ask of anybody, the step most likely to be forgotten, and forgetting it looks like the product being broken rather than like a missed step. The migration journal could not be used for this: it had one entry against twelve SQL files, because the files were hand-written and `push` was always the real mechanism. **The script adds and refuses to drop.** A schema push reconciles in both directions, and dropping a column is how an automated deploy turns into data loss that no test would catch — so if the database holds anything this build does not know about, it stops, prints the list, changes nothing and fails the deploy, because that is a question for a person. A missing `DATABASE_URL` is not a failure and lets the build through, since the public pages need no database and taking the marketing site down over a missing setting would be a worse outcome than the gap `/api/health` already reports. An unreachable database fails loudly. Proven against real databases in all six states before shipping: behind (migrates, 288 assessments and 42 criteria intact), already correct (no-op), holding an unknown table and column while behind (refuses, both survive), holding extras but not behind (no push, so no risk), unconfigured (build continues), unreachable (build fails).
- 2026-09-11 — **The deploy-time migration can never fail a build, and the build command survives the migration tool being absent entirely.** The first version of it exited non-zero when it could not reach the database or found something it would not touch — and that took a production deploy down, for a reason invisible from outside the build sandbox. `tsx` and `drizzle-kit` are devDependencies, so a build environment with `NODE_ENV=production` prunes them and the first command dies before anything is even attempted. Two corrections: the script now returns rather than exiting on every unhappy path, and the build script uses `npm run db:migrate || true; next build` so neither a missing tool nor a failed migration can stop a release. Nothing is hidden by this — everything the migration declines to do is reported by `/api/health` as a 503 naming the exact tables and columns. The lesson generalises: a helper that brings the database forward must never be able to stop the thing it was written to help.
- 2026-09-11 — **The repository had no automated checks at all, and that is the root cause of every surprise this project has had.** Every safeguard was somebody remembering to run it, and "verified locally, with a database, with dev dependencies installed" is not what a deploy environment looks like. `.github/workflows/ci.yml` now runs on every pull request and every push to main, and it deliberately reproduces the conditions that BROKE rather than the ones that work: typecheck, the full test suite, a build with **no** database configured, a build against an **empty** database (proving a new environment can create its schema from nothing), the same build again (proving the migration is idempotent), and then it starts the built app and asks it for pages — because "it compiled" and "it serves" are different claims and only the second one matters.
- 2026-09-11 — **`src/db` connects on first use, not on import.** It used to throw at module load when `DATABASE_URL` was missing, and almost every module imports `db` — so one absent setting stopped the production build while Next.js collected page data for an API route, and at runtime killed every page including the marketing ones that never touch a database. Connecting lazily puts the failure where it belongs: a page that reads nothing serves normally, a route that queries fails on that request, and `/api/health` names the missing setting. A Proxy keeps every call site unchanged, so this is a change of failure behaviour rather than something anybody has to write code around. **The CI above caught this on its first run.**
- 2026-09-11 — **Nothing on the request path may throw because an auth service is missing or slow.** `createServerClient(process.env.X!, ...)` threw "Your project's URL and Key are required" from inside a page render, surfacing as a 500 on every protected page; the middleware's unguarded `getUser()` would do the same during a Supabase outage, taking the marketing site down with it. `createClient()` now returns null when unconfigured — which makes the compiler force every caller to decide what the absence means — and the middleware is wrapped, bounded to three seconds, and returns the response regardless. Reading who is signed in treats an absent service as NOBODY, which is the only honest answer and sends the visitor to `/signin` instead of to an error page. Anything that WRITES — signing in, signing up, sending a link — still fails visibly, because silently doing nothing there would be worse. Verified with Supabase entirely unset: public pages 200, sign-in and sign-up 200, every protected page 307 to sign-in, nothing 500.
- 2026-09-11 — `tests/resilience.test.ts` holds these properties, including by reading the source as text for `process.env.X!`, a `try`/`catch` in the request-path middleware, and `process.exit(1)` in the deploy migration. Crude on purpose: these are one-character regressions that read as tidying up, pass review, and take the site down the next time a setting is rotated.
- 2026-09-11 — The brand arrives: the feathertail glider mascot and the SPEC mark, from the second design export (which also added Mascot, Logo Concepts and Colour System — three designs that did not exist in the first handoff). **The mark is built as a component, not shipped as a picture**, because the logo design is not a static logo: the O is the outcome and the clock, the claw is the hand, and where the hand sits is where the score sits. `SpecMark` takes a score and draws the ring travelling red → amber → green with the claw pivoting to match; at 100% the ring closes, the hand lands on twelve and stays, because a business that has finished does not go back to the beginning. `score={null}` is the brand at rest — the snapped state used everywhere outside the product. One object serves the favicon, both navigation headers, and any place a month is shown, which is the design's own instruction: "one idea rather than a logo and a separate set of charts". The C2PA provenance block was stripped from `logo.svg` (8,675 → 898 bytes) because it is served on every page. The glider sits on the front door beside the supporting line it illustrates, "nimble and powerful".
- 2026-09-11 — The front-door guard now counts the WORDS a visitor reads rather than lines of source. Adding the mascot and a two-column layout tripped a line-count test while the page still said thirty-four words; a guard that fires on layout gets its limit raised until it means nothing. The instruction being protected is "one line and one button", which is about the amount of argument on the page, so that is what is now measured.
- 2026-09-11 — **Option D, decided and applied: the pillar is a letter, and colour is only ever the score.** Inside the product a pillar carries no colour of its own — it is S, P, E or C — and the only thing coloured is how it is going: green, amber, red. The reason is that two colour systems on one card can contradict each other: a People card painted sage as its identity, with a red score inside it, shows two greens and a red meaning three different things, and a reader has to learn which green is which before they can read their own business. With one system that cannot happen, and it is colour-blind safe because the letter carries the identity. This reverses the four-pillar palette recorded on 11 September; the colours are **moved, not deleted** — `BRAND_COLOUR` still holds them for the logo, the public site (`/how`, `/sectors`, `/start`) and the printed board pack, which are the places SPEC presents itself rather than showing a business its own numbers. `PILLAR_META` lost its `colour` key entirely so the wrong thing is hard to reach, and `tests/colour.test.ts` reads the source as text to keep the four hexes out of every other file.
- 2026-09-11 — Colour uses the 90% rule (`scoreColour`), deliberately NOT `band()`. `band()` calls a pillar "on track" only at 100%, because a pillar is a set of measures and every one should be met — right for a LABEL, wrong for a light: a business holding 94% would see amber on every card while being told it is at the standard, which is the exact contradiction Option D exists to remove. The two live side by side on purpose.
- 2026-09-11 — **The login trap is closed.** The sign-in is created before the business, deliberately, so an address already in use is refused before anything is built — which left a gap: if anything after it failed, somebody owned an account attached to nothing. They could authenticate, every page found no seat and sent them to `/signin`, and `/signin` showed the form again. An unbreakable loop, with no message, at the exact moment a new customer decides whether to trust the product. Three changes close it: sign-up RESUMES when the account exists but no seat does (proving the password first, because an address must never be claimable by filling in a form); `/signin` detects the stranded state and sends them somewhere that can finish the job; and an already-signed-in person is sent into the product rather than shown a form that reads as though the last one failed. `tests/signup-resume.test.ts` holds all three.
- 2026-09-11 — **You do not pay for a house before seeing inside it.** `/look` builds a real tenant on the spot — org chart, four questions, a month part-marked with one pillar deliberately behind — hands the browser a token for it, and drops the visitor straight onto the chart. Nobody is asked for an email, a password or a card. Signing up afterwards KEEPS that business rather than starting them over: `claimLook` renames it and clears the token. The safety rests on three things: the token is 256 bits of randomness that exists nowhere but that tenant's own row, so it cannot be pointed at anybody else's business; it is deleted the moment a real person claims the tenant, which is what stops an old browser reaching a business once it belongs to a customer; and a visitor is READ-ONLY, enforced in `assertWritable`, the one chokepoint every write already passes through — which also means a visitor can never send an email, invite anybody or reach anything that bills.
- 2026-09-11 — The visitor sits in the top role's actual seat rather than being a user with no role, because a roleless viewer gets bounced off Today to the journey — the screen for a business still being built, and the opposite of what somebody came to see. A consequence worth writing down: their viewer then looks exactly like a customer's, so nothing about the USER distinguishes a look-around. `Shell` asks `lib/look` directly instead. A visitor is also offered "Finish looking" rather than "Sign out", since they never signed in.
- 2026-09-11 — Saying no is a real button that goes somewhere gracious, with no second ask, no discount and nothing to cancel. A product whose whole argument is that it tells a business the truth cannot start the relationship by being sticky — and an ask with only one comfortable exit is a trap that people can feel.
- 2026-09-11 — **Never tell somebody their password is wrong when the fault is ours.** Driving a real browser through sign-up and sign-in showed what a new customer actually sees when the authentication service cannot be reached: "That didn't work. Try again," then "Email or password doesn't match." Both are lies, and the second is the cruellest error a product can produce — a person retypes a correct password until they conclude they have forgotten it and that the product is broken. `signInWithPassword` now returns `ok | wrong | unavailable` and `createSignIn` distinguishes `unavailable` from `failed`, and both screens say plainly that it is our end and nothing they typed is wrong. A trap worth recording: changing the return from a boolean to a string made `if (!(await signInWithPassword(...)))` silently always-false, which TypeScript cannot see — every call site had to be read, not trusted.
- 2026-09-11 — Small things that decide whether a form feels effortless: the first field is focused so somebody can just type, the email field asks for an email keyboard on a phone, and the password can be SHOWN. Typing blind into dots is the most common reason a correct password fails — on a phone, with autocorrect, early in the morning — and the argument against showing it is weak, because the threat is somebody standing behind you and only the person typing can judge that.
- 2026-09-11 — SPEC is installable (`manifest.webmanifest`), so somebody arriving at work opens it like an application rather than hunting for a tab. `start_url` is `/today` rather than the front door: whoever installed it has already signed up, and landing them on a marketing page every morning would be a small daily insult. Somebody not signed in is sent to sign-in by that page itself, which is the right order — the app opens where the work is, and authentication is mentioned only when it is actually needed.
- 2026-09-11 — **The journey every customer takes is now proven in a browser, on every change.** It could previously only be verified by reading the code, because it needs a real authentication service — so the one journey somebody takes before they have any reason to trust us was the one journey nothing checked, and every report of it ended in a caveat. `scripts/fake-auth.mjs` speaks enough of the provider's HTTP API for the real client to talk to it unmodified, so what is tested is the shipping code path: no stub inside the app, no test-only branch in anything that ships, and it refuses to start outside `NODE_ENV=test`. `scripts/journey.mjs` then drives it: front door → one press inside a real business → read Today as a visitor → the decision → sign up → **it kept the business they were looking at** → still signed in → sign out → sign back in → a wrong password told it is wrong and never told the service is down. Fifteen checks, all green, wired into CI.
- 2026-09-11 — **Nobody is signed out for closing their laptop.** Auth cookies are kept for a year rather than for the browser session: somebody signs in once, works, locks the computer at five, opens it at seven the next morning and SPEC is already there with the day on it. A session cookie would have signed them out every evening for no security benefit anybody asked for — the session is still governed by the auth provider and signing out still works, so this only removes the accidental logouts. Sign out also left the main navigation: it sits last and quiet inside "Everything else", because the intended shape of a day is to stay in it, and nobody should sign out by mis-clicking on their way to something else.
- 2026-09-16 — **Read-only follows the money.** A lapsed business is locked only when it actually owes something: `readOnly = lapsed && billable > 0`, and `assertWritable` refuses on that rather than on `lapsed`. The two came apart the day the first seat became free. A business of one lets its card expire — Stripe gives up, the webhook writes `lapsed`, every page goes read-only — and the bill is A$0, so the one button on the screen, "Fix payment", opened a checkout that found nothing to charge for and returned them to the page they came from. A closed door and a key that does not turn, over a debt that does not exist. Being lapsed is Stripe's opinion about a subscription; owing something is a fact about this business today, and only the second is something a customer can act on, so only the second may lock a door. `isLapsed` — the Shell's banner, asked on every page — now asks the same question rather than reading the plan column alone, because a banner promising "read-only until the payment is sorted" above pages that save perfectly well is the same untruth as the generic failure screen that guard exists to have stopped. It costs a seat count on lapsed businesses and nothing on anybody else. The banner's button also goes where the fix actually is: a subscription that is merely unpaid needs a card, which is the portal, while a cancelled one no longer exists, so that business is sent to a fresh checkout. Two webhook changes hold the rest — `customer.subscription.deleted` now clears `stripeSubscriptionId` as well, since an id for a subscription Stripe has deleted is a claim that is no longer true and it is exactly what decides portal-or-checkout; and `invoice.paid` is handled at last, returning a lapsed business to `basic`. `lapsed` had three ways in and none out: a customer could put a new card in, be charged, and stay read-only until somebody noticed and changed a column by hand, with the product's own promise — "everything comes back the moment it is sorted" — kept only by a human remembering. Only a lapsed row is touched, so the ordinary monthly invoice of every other subscriber never overwrites a `beta`, a `program` or anything an administrator decided.
- 2026-09-16 — **The front door goes back to simple: design export 6.** The hero is the wordmark and the word "Simple." and nothing else. Two sections are deliberately gone, and both said true things: the crossed-out buzzwords with the AI argument under them ("Everyone's selling you AI. We harness it — with your people", "All that power, and nothing aiming it. SPEC puts the glasses on."), and the four-question grid ("One yes is enough to need a system"). They are removed because of WHEN they spoke, not whether they were right — both made SPEC's case before the page had earned the right to make it, which is the one thing this page is not allowed to do. Its whole argument is that it demonstrates before it claims, and three screens of positioning above the box is claiming. The argument is not lost, it is relocated to where it is stronger: a stranger types a real ongoing problem and watches SPEC read it, which proves the same point on their own business instead of asserting it about everybody's. The People-then-Compliance-then-Earnings order, which the removed prose carried, is stated by `components/problem-box` as the fix at the moment it is demonstrated. This reverses part of two commits from 13 September (`5e892e6` "Say the differentiator on the front door" and `0cca12a` "All that power, and nothing aiming it") — worth naming, because both were deliberate and neither was wrong about the content. The closing card loses its explanation of the KPI mechanism and becomes "Ready when you are.": by the bottom of this page a reader has typed a problem, seen the reading and seen the price, and what is missing is a door rather than one more explanation. The page is 2878px instead of 4215px. Design coverage goes UP, from 6 exact to 7 exact of the Landing screen's phrases, because the page and the artboard now agree about what the page is for. Recorded because the removed sections are the kind a future session would helpfully reinstate.
- 2026-09-20 — **A leadership seat was charging the team rate — not in the code, in Vercel.** Kris: *"when i go to pay it goes to $26."* `lib/pricing.ts`'s `STRIPE_PRICES.leader_basic` names the correct A$134 price, but `STRIPE_PRICE_SEAT_MONTHLY` — the env var that wins when set, per the note above `priceId` in `api/stripe/checkout/route.ts` — was still `price_1UGFj9GjbPN3KVS7PMWIRG3C`, set 16 September, before Design 15 split one flat seat into leadership and team on the 19th. Nobody updated it when the split landed, because nothing about landing the split touches Vercel — the checkout route just started reading an old id for a new purpose. Every leadership seat had been billing A$26 since the 19th; team seats were unaffected, since `STRIPE_PRICE_TEAM_SEAT_MONTHLY` was never set and so was already falling through to the correct hardcoded id. Corrected in Vercel to `price_1UHK06GjbPN3KVS7Erx7Aeum` and redeployed. **Worth naming as a class of fault**: this file and git history are both silent about it — no diff shows a wrong number reaching a customer, because the wrong number was never in the repo. A price the code computes correctly can still be the price nobody is charged, and the only place that would have shown was the Stripe dashboard or a customer's own card statement. Checked directly against Vercel rather than guessed at, per the standing rule in CLAUDE.md: never state what a live setting is without asking it.
- 2026-09-22 — **The Virtual GM Power Meter counts an unmeasured slot as 0, on purpose — narrowly.** Kris, mid-session: *"if something hasn't been done or measured it is a 0 - then brings down the score - until a score is entered or met."* Confirmed on being asked: the zero applies immediately, not phased in after setup, and **only to this meter** — core pillar/role/team scoring (`lib/scoring.ts`), which every incentive and the board pack read, keeps excluding NA from both sides of its fraction exactly as BUILD_SPEC §3 and the 2026-09-10 entry above say. This is a deliberate, scoped reversal of the general "pending is never red" / NA-exclusion principle for one instrument only: the meter is a nudge to go measure things, not a scored outcome anybody is judged or paid on, so a gap in what's measured is allowed to cost points here in a way it must never cost points there. `TOTAL_POINTS` is now fixed at 100 (five heavy hitters × 15, twenty shared slots sharing 25) and every reading is a real number — `PowerReading.score` is no longer nullable, `coverageLine` explains the gap instead of hiding it, and `SlotRow`'s three-state read (met / not met / not measured, not measured never red) is unchanged at the row level; only the total is affected. `power-meter.ts`, `power-meter.tsx` and their tests all updated together; `lib/scoring.ts` untouched.
- 2026-09-22 — **"Do you want SPEC AI powered?" is now a real, per-business question — JBI is the test case, not a special case.** Kris asked why JBI showed A$134 instead of A$227; A$227 is the Advanced (with-AI) leadership rate, and nothing in SPEC had ever offered it — `AI_TIER_ON_SALE` was a single product-wide flag, hardcoded `false` since 19 September specifically because flipping it would move every existing business onto the dearer seat at once with nobody having chosen anything. Asked whether JBI should just be special-cased, Kris: *"jbi is the test case — needs to follow all processes so the next businesses can do this on their own. there needs to be a question that says do you want SPEC AI powered — if yes then the leadership seat is this and if no then the leadership seat is that."* So the flag is retired and replaced with a per-tenant choice: `tenants.seatTier` (`basic` | `advanced`, new column, defaults `basic` for every business, existing and new — deliberately NOT a reuse of the pre-existing `tenants.tier` column, which already holds `'advanced'` on every row from a retired, unrelated meaning and would have silently moved everybody the moment it was read again). An administrator answers it on the journey page (`setBusinessSeatTier`), right above the price it decides. `checkout/route.ts` and the new `syncSubscriptionSeats` (lib/plan) both build their Stripe line items from the same shared `lineItemsFor` (lib/pricing), so a first checkout and a tier change on an ALREADY-subscribed business can never compute the bill two different ways — the exact fault class `seatBill`'s own history warns about. Changing the tier on a subscribed business pushes the new line items onto the live Stripe subscription with prorated billing (`reconcileSubscriptionItems`, matched by price id so an unrelated line's quantity is never touched for no reason), rather than only updating the row and leaving Stripe telling a different story. JBI answers the question itself, through this same screen, to reach Advanced — nothing here sets it for them by hand.
- 2026-09-22 — **Money moved off Journey onto its own "Pricing" nav item.** The AI-powered question landed inside Journey's setup steps in the previous entry, and Kris found it there twice — first needing the nav pill's link to Journey explained, then, after using it successfully: *"ok yes that works but not clear to do that."* Asked where it should go instead: *"put under pricing"*, then, choosing between options, explicitly *"Its own 'Pricing' nav item."* `/pricing` was already the public, unauthenticated marketing page (`src/app/pricing/page.tsx`) and stays untouched; the authenticated in-app page is new, at `/billing`, labelled "Pricing" on the bar. Everything that decides the bill moved there from `src/app/journey/page.tsx`: the BILLING_NOTICE banner, the `plan.free` and `plan.needsCheckout` blocks, `plan.billing`'s Stripe-portal link, and the "Do you want SPEC AI powered?" question itself (`setBusinessSeatTier` moved to the new `billing/actions.ts`, `revalidatePath` retargeted). Journey keeps only `plan.readOnly` as a pointer — a business locked for non-payment needs a way out from wherever it lands — now linking to `/billing` instead of holding the fix inline. `lib/doors.ts`'s nav bar goes from seven items to eight (`tests/doors.test.ts` updated to match), and every redirect in `api/stripe/checkout/route.ts` and `api/stripe/portal/route.ts` (`success_url`, `cancel_url`, `billing_error`, `no_subscription`, `nothing_to_bill`, the no-tenant fallbacks) now returns to `/billing`, not `/journey`. Functionally this only relocates a feature already proven working on JBI in the previous entry — the toggle, the tiered price and the prorated Stripe update are unchanged; only where somebody finds them moved.
- 2026-09-22 — **Nav bar: Setup as tab one, Pricing moved up next to Org chart, and the header stopped stretching to fill the row.** Kris, looking at the built bar right after the Pricing item landed: *"i dont understand whats happening - tab 1 is supposed to be set up - then my page - org chart - pricing and so on."* The bar is now Setup, My page, Org chart, Pricing, Scoring, Board pack, Mirrors, Connections, All pages — nine items, up from eight. `/setup` (the existing "Setting up" door, shortened the same way `/billing` already shortens to "Pricing" on the bar) is now tab one; My Page keeps its other two jobs — the logo's destination, and the address most of the directory describes itself as opening from — but is no longer first on the bar itself. Separately, Kris: *"why is everything so far right - line it up nicely"* — the header's `justify-between` stretched the mark and the nav to opposite ends of a 1152px row, leaving a wide dead gap on anything wider than the content. Removed; the mark and the nav now sit as one left-hand group, and only the billing pill/bell/business-name block is pushed right on its own `ml-auto`, which it already had. `lib/doors.ts` and `tests/doors.test.ts` updated together (nine items, new label order, new first-item and bar-vs-directory tests); `src/components/ui.tsx` is the only layout change.
- 2026-09-22 — **The incentive ladder says "incentives" in its own heading, and every setup page under `/setup/*` now hands off to the next step.** Two small fixes from the same look at the built product. First, Kris on the Administration screen: *"this needs to be clear we are talking about incentives"* — "What a month can earn" read as base pay. It is now "What a month can earn in incentives", the paragraph states "on top of base pay, never instead of it", and the Save/Restore buttons and their captions say "incentive ceilings" rather than bare "ceilings". Second: *"also when going through the setup phase it should link nicely to the next step."* `/setup/expectations` and `/setup/focus` already did this — `<Interview>` finishes on a "What happens next" callout naming and linking the next journey step, rather than stopping cold. That pattern is now a shared `<NextStepCallout>` (`src/components/next-step.tsx`) and a shared `nextStepAfter(tenantId, currentHref)` (`lib/journey.ts`, the same "first undone step, excluding this one" the expectations page already computed by hand), and it replaces the bare "Back to the journey"/"Back to setup" links at the bottom of `/setup/business`, `/setup/kpis`, `/setup/board`, `/setup/systems` and `/setup/goals`. `<Interview>` itself now renders through the shared component too, so the two copies of that markup cannot drift apart again.
- 2026-09-22 — **The Basic/Advanced seat-tier split from earlier today is reversed. One price per seat kind again, and AUD's team seat moves to $26.** Kris, looking at the built result of the previous three entries: *"i also feel like ii don't want to have 2 different prices. either use the system or not - make it simple - I think take all prices for seat to $134 and for team members $26 - feels too confusing to me."* Asked whether that applied to every currency or only AUD: *"AUD only; others keep their own."* Asked to confirm the tier was gone rather than merely equalised: *"make it either on or off - then the prices are clear - the system seats are $134 and the member seats are $26."* "On or off" was already answered by something the question never touched — `aiActive` in `lib/plan` (subscribed, program or beta) was never read from `seatTier`, so every subscribed business got the assistant either way and the two prices never bought two different products. `tenants.seatTier`, `setSeatTier`, `syncSubscriptionSeats` and `reconcileSubscriptionItems` (all built earlier today) are retired; the column stays in the schema, unread, the same way `tenants.tier` does. `SeatPrice` drops `leadershipWithAi`/`teamWithAi`; `SEAT_PRICES.aud` is now `{ leadership: 134, team: 26 }` (leadership unchanged, team up from 17) and every other currency keeps the numbers it already had. `RULE_OF_EIGHT` is now `[17, 26, 134]`. `PACKAGES.seat_training` (the public-page package sharing this same confusion) is retired the same way — `publishPrice: false`, `aud` equal to `PACKAGES.seat.aud` — kept rather than deleted because the key is stored on businesses. **One gap remains, and it is Stripe's, not the code's:** a Price object cannot be edited once created, and the live account has no $26 AUD team price — only the $17 `team_basic` id from 19 September. `STRIPE_PRICES.team` falls back to that id until a genuine $26 price is created and its id substituted, so checkout will charge $17 for a seat the page shows as $26 until that happens. `docs/STRIPE_SETUP.md` names the gap and what the new price needs (same currency_options as the existing row, only the AUD default changed). Updated together: `lib/pricing.ts`, `lib/plan.ts`, `api/stripe/checkout/route.ts`, `billing/page.tsx` and `billing/actions.ts` (the "Do you want SPEC AI powered?" block removed), the public `/pricing` and `/how` pages (the four-column table and the Advanced/Basic J-curve paragraph removed), `db/schema.ts`'s column comment, `lib/doors.ts`'s `/billing` note, and every test that touched the retired tier.
- 2026-09-22 — AUD's team seat stays $17, reversing the $26 this same tier-retirement session had just set. Kris, once the Basic/Advanced tier itself was gone: *"oh yeah stay at 17 that sfine."* This conveniently removes the gap the previous entry recorded rather than deepening it — Stripe's live `team` price was already $17, so the code now matches the account exactly with nothing left to create. `SEAT_PRICES.aud` is `{ leadership: 134, team: 17 }` again; `RULE_OF_EIGHT` drops back to `[17, 134]`. Updated together: `lib/pricing.ts`, `lib/plan.ts` (a stale comment), `tests/pricing.test.ts`, `tests/packages.test.ts`, `tests/training-seat.test.ts`, `docs/STRIPE_SETUP.md` (the gap section removed), and a stale `A$26` figure in `billing/page.tsx`'s `training_price_missing` notice comment, left over from before the leadership/team split.
- 2026-09-22 — The retired $227 leadership-with-AI price becomes the leadership seat's training upgrade. Kris, once the tier itself was gone: *"i think the 227 price can stay but change to full training system price — so all training materials become available for the role — we as SPEC are constantly building our training materials so they can turn the seat to a leadership and training seat and that then makes it 227."* Confirmed per-person (not a whole-business switch) and on sale now (not gated behind a finished-pack flag, unlike the earlier $44 supervisor mechanism it replaces). Same live Stripe price and product (`price_1UHK3PGjbPN3KVS7vot0UtCu` / `prod_VHtrdn6wF9T8JM`), un-retired from `RETIRED_STRIPE_PRICES`/`RETIRED_STRIPE_PRODUCTS` into `STRIPE_PRICES.leaderTraining`/`STRIPE_PRODUCTS.leaderTraining` — nothing created or changed in the account, only what the price is sold as. `SEAT_PRICES` gains `leadershipWithTraining` (227/305/149/227/227/305); `RULE_OF_EIGHT` gains 305. Eligibility broadens from role LEVEL (`supervisor` only, via the retired `TRAINING_LEVELS`/`canBeTrained`/`isFrontlineLeader`) to seat KIND (`eligibleForTrainingSeat`, built on the same `seatKindFor` billing already uses), so a person's eligibility can never disagree with how they are actually charged. `seatBill`/`lineItemsFor`/`planState`/checkout are wired for the first time to actually bill it — the old $44 mechanism's `countTrainingSeats` was built but never called by anything that charged Stripe; the new one is. The per-person admin control moves from Settings to a new panel on the existing Training screen (`lib/doors.ts` already listed a "Training" door), resolving the naming clash with `PACKAGES.sessions` (the unrelated $1,502 "SPEC Training" one-to-one package) by keeping the new panel's own copy distinct rather than reusing that label. Updated together: `lib/pricing.ts`, `lib/plan.ts`, `lib/training-seat.ts`, `lib/chart-seats.ts` (unchanged, now reused), `db/schema.ts`'s column comment, `app/api/stripe/checkout/route.ts`, `app/training/page.tsx` and `actions.ts`, `app/settings/page.tsx` and `actions.ts`, the landing page's pricing card, `docs/STRIPE_SETUP.md`, and every affected test.
- 2026-09-23 — Org chart cards: role titles and the person's name are bigger and bold, not the plain 16px/12.5px body text they were. Kris, looking at a live chart carrying a real new hire: *"on the org chart the role and persons name are not clear - need to make these bigger and bold."* This reverses part of the 19 September call to match the design's own preview rendering ("match the design preview - make titles the body font") — right for legibility against the display face, wrong for reading forty cards at a glance once real people are on them. `CARD.title` (`lib/orgchart.ts`) moves 16→18 and the title span gets `font-semibold`; the person pill moves 12.5px→14px, `leading-[18px]`→`leading-[19px]`, and gets `font-semibold` (the vacant-role text grows to the same 14px/19px so the row's height math still holds, but stays plain — there is no name to make bold). `cardHeight` derives from `CARD.title` and `PERSON_ROW` rather than a typed number, so both are updated in the same commit and the two cannot drift the way they did on 18 September ("Head of Commercial" silently losing its second word). `tests/orgchart.test.ts`, `tests/chart-seats.test.ts` and `tests/chart-text.test.ts` (112 tests) still pass unchanged; `scripts/org-journey.mjs`'s line-height-ratio and two-line-box checks hold because they read the computed style rather than a hardcoded number.
- 2026-09-23 — A stated seat-kind override joins the chart's own read, for the same business the bold-text change above shipped for. Kris added Janine, an Ops Admin with nobody reporting to her, and the chart's own rule (`seatKindFor` — title matches a leader word, or somebody reports to the role) billed her as leadership anyway: *"this isn't a leadership seat this is a team seat - Ops Admin Janine - so how do we set that properly and bill it properly because now it says $268 per month instead of $151."* Working as designed and still the wrong bill, since a chart-only rule has no way to hear "no, not this one." He then asked for more, in the same conversation: *"we need the capacity to chose whther leadership seat or team seat when sending their email to join - then they see they are joining a leadership or team memvber seat."* Offered the choice between the invite screen merely showing the chart's computed answer or a genuine override, he picked the override: **"Let me override it by hand per invite."**

  `users.seatKindOverride: 'leadership' | 'team' | null` — null defers to the chart exactly as before and self-corrects the moment the chart changes (Janine's own case, fixed for free by placing her correctly); set, it is a stated choice that wins until changed by hand, deliberately able to disagree with the chart from then on. `resolveSeatKind(role, override)` in `lib/chart-seats` is the one place this is decided — billing (`lib/plan`'s `leadershipSeatCounts`), training eligibility (`lib/pricing`'s `eligibleForTrainingSeat`), and the org chart's own badge (`seatBadges`) all call it, so the bill, the badge and training eligibility can never land on three different answers even though one of them can now disagree with the chart. The invite email says which one plainly — *"they see they are joining a leadership or team member seat"* — computed the same way (`lib/invite.ts`'s `inviteToSeat`, `lib/email.ts`'s `sendInviteEmail`), so nobody finds out from a bill.

  Straight after, a narrower question: *"make it that only someone in a leadership seat can invite someone to join the org chart - they then decide if this is a leadership seat or member seat."* This replaces "any manager with full/administrator access in their own branch" with something stricter: a new `scope.canInvite` (`lib/scope`'s `mayInvite`) is true only when the CALLER's own resolved seat is `'leadership'` — by title, by the chart, or by their own override — or they are an unplaced administrator (the same day-one founder carve-out `mayShapeChart` already had, so a founder is never locked out of inviting their own first hire). That one gate covers both halves at once: whether somebody may invite at all, and whether they may choose the invitee's seat kind or change an existing seat's override afterwards from the `/org` card — deliberately the same question, per Kris's own framing ("they then decide").

  Threaded through `db/schema.ts`, `lib/chart-seats.ts`, `lib/pricing.ts`, `lib/plan.ts`, `lib/training-seat.ts`, `lib/invite.ts`, `lib/email.ts`, `lib/queries.ts`, `lib/orgchart.ts`, `lib/scope.ts`, `app/org/page.tsx` and `actions.ts` (new `setSeatKind` action), `app/setup/people/actions.ts`, `app/setup/business/page.tsx`, `app/training/page.tsx`, and `components/org-canvas.tsx` (the invite form now offers a seat-kind choice; a selected, already-placed seat gets a second "Billed as" control, auto/leadership/team, showing the chart's own answer alongside any override). CLAUDE.md's pricing section, which said "never a field anybody sets", is corrected in the same commit. 1483 tests now pass (up from 1472): new coverage in `tests/chart-seats.test.ts` (`resolveSeatKind`, `seatKindFromForm`, `seatBadges` with an override), `tests/scope.test.ts` (`mayInvite`), and `tests/training-seat.test.ts` (training eligibility following the override).
- 2026-09-23 — A live subscription now follows the chart on its own, rather than waiting for a checkout. Right after the seat-kind override shipped, Kris: *"yes stripe needs to auto sync as I could never keep up if there are 100's of companys."* Until now a subscription's quantity was set once, at the moment of checkout (`api/stripe/checkout/route.ts`), and nothing ever touched it again — a business that grew from six leaders to nine, or moved somebody like Janine onto team, kept paying the day-one number until an administrator opened Stripe by hand and fixed it. Fine at one business; impossible to keep up with at a hundred.

  `syncSubscriptionSeats` (`lib/plan`) pushes the current seat counts onto a business's live Stripe subscription, matching existing line items by price id and touching only what actually differs — a business that only grew a team seat should not see its unrelated leadership line "change" for no reason a customer could explain from their invoice. It is called after every write that can change what a business owes: `lib/invite`'s `inviteToSeat` (a new seat), and, in `app/org/actions.ts` and `app/setup/people/actions.ts`, every action that moves a person, swaps two, reparents a role, vacates one, or changes a seat-kind override. A business that has never subscribed is a no-op — nothing to reconcile, and its first checkout simply reads the chart as it stands then. Best-effort throughout, the same shape as `email.ts`'s `send`: it swallows its own errors and logs rather than throwing, so a Stripe hiccup can never break the org-chart edit that triggered it, and the next write anywhere on the chart tries again.

  This exact mechanism shipped once before, briefly, as `syncSubscriptionSeats`/`reconcileSubscriptionItems` behind the Basic/Advanced seat-tier choice (22 September), and was retired with that tier the same day when Kris collapsed it back to one price per seat kind. The diffing was never the problem — only the second price it existed to push was premature. Revived here for the reason it was always going to be needed for.

  Writing the test for it surfaced a real, separate, pre-existing bug: `lineItemsFor` (`lib/pricing`) read `seatBill`'s `leadership` count — which is deliberately plain-and-trained seats ADDED TOGETHER, so `leadership + team` is always every billed seat — straight onto the plain leadership price, and then added a second line for the trained seats at the training price on top. A business of forty with six leaders, two of them trained, was one call away from being billed for eight leadership-rate seats instead of six. Nothing had ever exercised a non-zero training count through this function; there was no test for it at all before `tests/pricing.test.ts`'s new `lineItemsFor` block and `tests/subscription-sync.test.ts`. Fixed in the same commit: the plain leadership line now excludes whoever is already being counted on the training line.

  1494 tests now pass (up from 1483): `tests/subscription-sync.test.ts` (new — `reconcileSubscriptionItems`) and `tests/pricing.test.ts` (new `lineItemsFor` coverage, including the double-billing regression). CLAUDE.md's pricing section and `docs/READINESS.md`'s test count are updated to match.
- 2026-09-23 — Changing what somebody is billed as no longer requires finding their exact card on the org chart. Kris, having just been walked through clicking Janine's box on a forty-card chart: *"this should be possible to change from any leadership seat - this is too hard - make it simple."* The control already existed — `setSeatKind` on the org chart card — but the only way to reach it was to locate the right box among however many the business has, which does not scale past a handful of people.

  `/billing` now lists every person with a SPEC login by name, sorted for scanning rather than laid out as a diagram, each with the same "Billed as" dropdown the chart card has (Auto / Leadership seat / Team seat), posting to the same `setSeatKind` action — one control, reachable from two screens, never two copies of the decision. Gated by `scope.canInvite`, the same leadership-seat rule as everywhere else this decision is made: anybody who cannot invite sees the current kind read-only, with a sentence saying who can change it. The chart card itself is unchanged and still works exactly as it did — this is a second, easier way in, not a replacement.
- 2026-09-23 — The bare address is siteVIP, the trades edition of SPEC (`designs/siteVIP Landing.dc.html`), and SPEC's own front door — problem box, reading, proof, pricing — moved whole to `/spec`, linked from the siteVIP page and the public nav. Both keep the guarded signed-in redirect to My page. The business name typed on siteVIP travels to `/signup?business=` (the parameter the problem box already used), so it is never asked for twice. The design named four products in its "keep what you have" list and flow; the never list wins, so they are categories (`My job system`, `My accounting`, `My CRM`, "your accounting system is updated") — see `lib/sitevip`. Canonicals resolve against `www.sitevipapp.com` via `metadataBase`, with `robots.ts` and `sitemap.ts` added.
- 2026-09-23 — `/tech-day` (designs/SPEC Tech Day.dc.html) enforces an order the design did not: no Start before the SWMS is signed, and photos, materials and sign-off only after Start, since the design's tap-anything would let hours be missing from the job materials were costed to. Rules in `lib/tech-day`. There is no jobs, SWMS, photo or timesheet table yet (Jobs is being built separately), so the tech names the job, the day is kept on the phone (localStorage, per person per day), and every screen says so. When the store lands, the job list comes from the schedule and each tap is written through; the steps do not change.
- 2026-09-23 — People gains Reviews & conduct and Pay & exits (design 23 Sept), built only from what SPEC already holds: a review is the last three months of the person's KPI board; a contract is drafted from the chart role, with pay and the award level left to the business; an exit is a closed placement whose person holds no other open one (a move between roles is not an exit). The award is "the business's award", which it names — no award, regulator or statute is written into the product. Warnings, exit reasons, the award's name and payroll hours have no store yet, so those cards show their structure and say plainly nothing is recorded. No tables added. See `lib/hr`.
- 2026-09-23 — `/coverage`, the capability map (38: Jobs 17, HR 10, Safety 11), lives in `lib/coverage`. The design names one business's vendors and defaults every Jobs row to that business's job system; the product names categories ("Your job system", "Your accounting system", "Your CRM") and starts each switch from the connections the business has already made. There is no per-tenant settings store (tenants carries named columns only), so the choice is kept in the browser, per business, until one exists — the page says so.
- 2026-09-23 — The Power Meter breakdown names the source of every measure (`Slot.source` in `lib/power-meter`); all five heavy hitters come from SPEC's own records. The same design export reverted the tagline to "Hack Your Power"; the word stays HACC (Kris, 22 September) and the export's wording is recorded in `designs/superseded.md` rather than reverted in the product.
- 2026-09-23 — **Jobs (`/jobs`) runs on the business's own records: eight new tenant tables.** Built from design `SPEC Jobs` (23 September). `jobs`, `quotes`, `quote_lines`, `catalogue_items`, `labour_rates`, `kits`, `schedule_bookings`, `timesheet_entries` — each with its own `tenant_id`, each under the tenant policy in `drizzle/0001_rls.sql`, added as one block at the end of `db/schema.ts`. Real today: the pipeline (enquiry → paid, one stage at a time), the live quote builder, the crew × day schedule, timesheets and the catalogue with pasted supplier price files and kits. Drawn in the design's shape but honestly empty, each with a first step: stock & buying, invoices & claims (except "who owes what", which is read from invoiced jobs), and service & assets. Decisions inside it: **money is integer cents** and a line is rounded once; **a quote line is a snapshot** — the catalogue price is copied onto it when added, and a sent quote is never repriced (a change is a revised quote); **the browser never sends a price** — `saveQuote` rebuilds every new line from the catalogue on the server; **markup applies to materials only**, labour sells at its charge rate; **GST 10% on the total**; **margin is judged against a 40% benchmark** (green at or above, amber within ten points, red below; pending when there is nothing to measure) and **billable share against 86%**; **a job's margin is null until costs land on it**, never a flattering 100%; labour to date is timesheet hours at the business's first labour rate. **SPEC never emails a quote** (rule 5) — "Quote sent" records that it went out and moves the job to Quoted. **Clear to Work gates the schedule** by the same path as /people, checked on the server at every booking; somebody *not clear* cannot be booked, while somebody *not yet established* (pencilled, nothing recorded) can, shown in grey — refusing every name on the chart that is not yet a seat would make the schedule unusable rather than safe. The design's "Switchboard upgrade" appears as a clearly labelled, interactive, never-saved worked example. Vendors by category only; the business's own suppliers are whatever it types. Arithmetic in `lib/jobs.ts`, tested in `tests/jobs.test.ts`.
- 2026-09-23 — **The CRM (`/crm`) is SPEC's own, with the workflow of a sales pipeline and none of anybody's branding: six new tenant tables.** Kris: *"copy [a named CRM product] for the CRM"* — the workflow, not the name, colours or copy; the product never names it, and `tests/crm.test.ts` fails if any CRM vendor's name reaches the CRM's sources. `crm_stages`, `crm_organisations`, `crm_people`, `crm_deals`, `crm_activities`, `crm_deal_events` — each with its own indexed `tenant_id`, each under the tenant policy in its own block at the end of `drizzle/0001_rls.sql`, appended as one block at the end of `db/schema.ts`, **no foreign keys** (the shape production has; every id from a form is re-read with the tenant first), money in whole cents. Real today: the deals board (drag a card between columns, or the "Move to" list on every card for phones and keyboards), Won and Lost (Lost from a short list, with words required for "Other"), Reopen for a lost deal, deal detail (value, expected close, probability by stage, owner, organisation and person, notes, the history of every stage change, scheduled activities with a done tick), the Activities list (overdue first, then today, then coming up; yours or everyone in your line), People and Organisations with search and each one's open deals, and the stages themselves (rename, chance of winning, days before quiet, add, remove once empty). Decisions inside it: **the forecast strip is computed on every read and never stored** — open value, weighted value (value × the stage's chance), won this calendar month, and win rate over the last 90 days, which reads "not measured yet" rather than 0% when nothing has closed. **SPEC proposes the five trade stages** (Lead in · Contacted · Site visit booked · Quote sent · Negotiating, 10/20/40/60/80%, seven days before quiet) and they are only written the first time a deal or an edit needs them — reading the page never writes. Reshaping the pipeline is the top of the chart's or an administrator's, because the stages are the whole business's. **Lights:** a deal with nothing happening for its stage's days is amber ("Quiet 9 days") — pending is never red; an activity past its date is red, because it is a promise already missed; nothing scheduled is grey. Scheduling something is not something happening: a deal whose only sign of life is a promise is still quiet. **Who sees what:** a deal is owned by a ROLE on the chart (with the holder's name kept beside it), and it is visible to that role and everybody above it in their own line — never sideways — by the same `lib/scope` walk every scorecard uses; a deal can only be given to somebody on the giver's part of the chart; a deal with no owner on the chart is an administrator's to place. Writes take the same gate as Jobs (`requireManager` + `assertWritable`) and then refuse any deal outside the writer's line. Organisations and people are the business's shared client list, the way a job's client is; what anybody sees OF them — their deals — is still only their line. **An "email to send" is a reminder, never a message** — nothing is composed or sent from the CRM (rule 5), and a test holds that.
  
  **Won becomes a job, and the CRM does not duplicate Jobs' enquiry stage.** Jobs already has a front door — an enquiry typed in one line lands at `enquiry` — and that stays exactly as it is: a call that is plainly a job (a service call, a repeat client, a small fix) is still typed straight into Jobs and never touches the CRM. The CRM is for the selling that happens *before* there is a job — the lead that needs chasing, a site visit, a quote argued over — which Jobs' pipeline has no room for without filling its board with work that may never happen. The two meet at one point: marking a deal Won creates the job through the **same creation path** a typed enquiry takes (`createJob` in `lib/jobs-data`, which `addEnquiry` now also calls), at stage **Won** rather than Enquiry because the selling is done, carrying the client (the organisation, else the person), the site and the value, taking the next J-number from the same series. The deal keeps the job's id and links to it; the job's detail in Jobs links back to the deal. Pressing Won twice never makes two jobs, and a won deal cannot be reopened from the CRM — it is a job now, and it is changed in Jobs. The coverage map's "Customers & sites" capability, whose connected option is "Your CRM", now opens `/crm` for SPEC's own; `/crm` is a door under "The work" and is deliberately not on the top bar (that is Kris's call). Arithmetic and rules in `lib/crm.ts`, tested in `tests/crm.test.ts`; exercised against a real Postgres (every action, both refusals of sideways access, won-to-job, the stage gate) before shipping.
- 2026-09-23 — **Coverage is real: the choice is kept per business, SPEC is the default, and every area can be handed to the business's own system in one press.** Kris: *"the system should provide the solution for hr and safety and jobs etc - but they can choose to use their own system if they choose and connect it - up to them but of course i think my system is the best but they must have options and simplicity"*. Until now the switch lived in one browser and changed nothing. Now: **`coverage_choices`** (tenant_id, capability, choice 'spec'|'own', updated_by, updated_at; unique on tenant + capability; no foreign keys; under the tenant policy in its own block at the end of `drizzle/0001_rls.sql`, appended at the end of `db/schema.ts`) holds one row per capability changed from SPEC — **no row is SPEC**, so switching back deletes the row and a business that never opens Coverage has nothing written. This replaces the earlier start-from-your-connections default: every capability starts in SPEC, because SPEC is the solution and their own system is the option. Only a manager changes it — `requireManager` + `assertWritable`, the same gate as Jobs and Safety — and everybody else sees the map read-only. **Simplicity first:** each area (Jobs, People and HR, Safety) has one "Use SPEC" / "Use my own system" switch above its rows; SPEC is said quietly as the default ("SPEC — built in, nothing to connect"); the per-capability rows stay for anybody who wants a mix. The CRM is not an area of its own in `lib/coverage` — it is "Customers & sites", one capability of Jobs — so it follows that one row. **Every row can now be handed over**, departing from the design, which offered another system only on the rows where one business happened to run one: the design's rows keep the kind it names (job system, CRM, accounting system for payroll export) and every other row falls to its area's kind — job system, HR system (filed under `payroll`, "People and payroll", board-approved as before), safety system. **Own system → connect it:** a handed-over row or area shows "Connected" when a connection of that kind is live on Connections, and otherwise a plain "Connect your HR system →" (by kind, never a product) that opens Connections' add form with the kind already chosen — never red. **The modules respect the choice without hiding anything:** Jobs, Safety, People and the CRM show one line at the top of the tab when that part runs in the business's own system — "You run jobs in your own job system. Connect your job system → · Switch back to SPEC" — and the SPEC screens keep working underneath. The line says where it runs and never that anything arrives, because no connector reads a job, safety or HR system yet (`tests/no-false-feed.test.ts`). The Power Meter's source labels follow the choice ("from your safety system") for the measures that read exactly one of the 38 capabilities; the number itself is untouched. The People page's "Already have an HR system?" card named two HR products; it now says "Connect your HR system", and `tests/coverage.test.ts` fails if any HR, job, safety, CRM or accounting product name appears in any page or component (Connections excepted, where the one real connector is named because it speaks that company's API). Pure logic — areas → capabilities, effective choice with the default, the tab lines, source labels — in `lib/coverage`; reads and the write in `lib/coverage-data` and `app/coverage/actions.ts`.

- 2026-09-23 — **The free seat is the first leadership seat.** Kris: *"remember in the costing the first person to sign up gets full access but isn't costed — so for JBI right now should be me as the originator then Anthony paid leadership seat and Janine paid member seat"*, then *"the free seat for each company is the first leadership seat."* This reverses the earlier rule (free seat off the cheapest, a team seat, first). `seatBill` now takes the free seat off a plain leadership seat, then a trained one, and off a team seat only when a business has no leader. JBI goes from A$268 (2 × A$134) to A$151 (A$134 + A$17). Pricing shows the free seat against the leader at the top of the chart, labelled "free — started the business".
- 2026-09-23 — **Full staff lists, full client lists, contacts, and the ability to do the job.** Kris: *"we must have full staff lists, we must have full client lists, we must have contacts and the ability to do the job"*. SPEC provides each itself; a business that runs customers in its own CRM still chooses that on Coverage ("Customers & sites"), and the SPEC screens keep working underneath. No new tables — seven additive, nullable columns on tables already under the tenant policy: `users.phone`, `users.start_date`, `staff.phone`, `staff.email`, `staff.start_date`, `jobs.organisation_id`, `jobs.person_id` (indexed with the tenant). Pure logic in `lib/directory`, `lib/clients`, `lib/csv` and `lib/tech-day`; tested in `tests/directory.test.ts`, `tests/clients.test.ts` and `tests/tech-day.test.ts`; every action exercised against a real Postgres before shipping.

  **Staff list — a tab of People (`/people?mode=staff`), not a new address.** People is already on the bar and already "the HR system"; its first tab, "Who you have", stays the viewer's own line, one name per role. The staff list is everybody in the business, **one row per person** — leaders, every member of a pooled team, and names not on the chart yet — with role, who they report to (by name and role), phone, email, start date (stated, else "on the chart since" their first placement, and it says which), and a link to the scorecard where the viewer may open it. **The reading of "only me and above" here:** a plain directory — name, role, reporting line, phone, email, start date — is visible to everybody in the business, because knowing a colleague's number is not seeing their scorecard and the chart's structure was never a secret (`lib/scope`). **Clear to Work and licences are not directory:** they are the working under a hard gate, read from the Compliance pillar and the person's own papers, so they show only for the viewer and the people beneath them — and on a pooled team, a technician sees their own and never a teammate's (sideways). Outside that line the cells are empty, on screen and in the CSV, never a guess. The status shown is `crewFor`'s — the very function the schedule refuses a booking with — so the list and the schedule cannot disagree. Details are changed by the person themselves (anybody may keep their own number right, read-only included), by a manager for somebody in a role they manage, or by an administrator for somebody not on the chart yet; a login's email is its sign-in address and is never changed from here. An email kept for a pencilled name is the one their invitation then offers (`/setup/business`), so it is never asked for twice. CSV at `/people/staff.csv`.

  **Clients — its own address, `/clients`, on the bar after CRM; no separate Staff item.** Chosen over a fifth CRM tab because the CRM is the selling before there is a job, and plenty of clients never pass through it — the service call typed straight into Jobs, the builder ringing with the next one. The client list is the CRM's organisations and homeowners (a CRM person with no organisation) **and every job's client**, seen whole, nothing copied. A job belongs to a client by its link, else by an exact name match (case, spacing and a trailing full stop aside — nothing cleverer), else it is listed as "on jobs only" under its own name, so nothing the business ever typed falls off. **Every new job is linked at creation** through the one creation path (`createJob`): a client picked on /clients or carried from a won deal keeps its link; a typed name joins the client of that name, and a name nobody has yet becomes a client so the next job links on its own; "New client" (no name given) is never filed as one. **Existing jobs are linked in one press** ("Put them on the list"), additively: only a null link is ever filled, only on a single exact match, a name nobody has becomes a client once, and the words on a job are never rewritten. Each client shows its sites (address, then every job and deal site, once each), contacts, open deals (only those in the viewer's line — `canSeeDeal`, unchanged), live and past jobs, and money as Jobs already knows it, ex GST: invoiced = jobs marked invoiced or paid, owed = invoiced and not yet paid. The CRM's Organisations and People tabs stay, and point here. Coverage's "Customers & sites" now opens `/clients`. CSV at `/clients/clients.csv` and `/clients/contacts.csv`.

  **Contacts** (`/clients?tab=contacts`) — every person at a client, homeowners, and every supplier the catalogue names (the catalogue keeps a supplier's name only, and the page says so), each with their client, jobs and open deals. **Tap-to-call and tap-to-email:** `tel:` and a bare `mailto:<address>` — no subject, no body, nothing attached — which opens the person's own mail app. SPEC composes and sends nothing (rule 5 stands; `tests/clients.test.ts` holds that no `mailto` here carries anything). The CRM's own sources still carry no `mailto` at all, as its test requires.

  **The ability to do the job.** From any client or contact, one press — "Start a job" or "Start a quote" — creates the job already knowing the client, the contact and the site (the first known site, or pick another); what the work is may be typed and need not be ("Work for Harbour Builders" until somebody says). "Start a quote" goes straight into the quote builder through Jobs' own `startQuote`. From a job, "Book somebody" books a person from the crew list without leaving the job, through the same `bookCrew` action and therefore the same server-side Clear to Work refusal; the disabled option for somebody not clear is a courtesy, never the check. A job's client name links back to its client.

  **Tech Day now runs on the office's schedule and writes real timesheets.** Today's jobs are the tech's `schedule_bookings` (by their login, and by the staff row they were pencilled in as before their invitation, so a booking made before they had a login still lands). **Start opens a `timesheet_entries` row and Finish closes it** (`app/tech-day/actions.ts`, `source: 'phone'`), waiting for the leader's weekly approval on Jobs → Timesheets as before. This is the one time-write a person without manager access makes, and it is narrow: always the signed-in person's own time (the person is never taken from the phone), a job goes on the entry only when the office booked them on it around today (a job they name themselves is hours not on a scheduled job, not billable), pressing Start twice opens one entry, **the minutes are the server's own clock** from Start to Finish (the phone's clock only labels the entry "07:14 → 08:49"), and a forgotten Finish caps at a day for a leader to fix. No signal never stops work: the phone keeps the day as before and says, at Start and on the done screen, when the office did not get the hours. **Still only on the phone:** the SWMS signature, photos, materials used and the client's sign-off — SPEC has no store for any of them yet, and the phone says so where somebody might think otherwise.

- 2026-09-23 — **The subscription sync can no longer fail silently, and the bill is one count.** After the free seat moved to the first leadership seat, /billing showed JBI at A$151 while the live subscription still charged 2 × leadership, and two presses of Save on /billing changed nothing with nothing in the logs. Three ways that could happen, all closed: (1) **two counts** — `/billing` itemised from active roles, while the total, checkout and `syncSubscriptionSeats` counted every role ever drawn; a removed role keeps its reporting line, so it could make somebody a leader for Stripe only, and when that count equalled what Stripe already charged the sync found nothing to change and returned. Now `classifySeats` (lib/plan) is the one rule: active roles only, team nodes are team seats unless overridden, account placement beats staff-linked, a person on two roles is decided deterministically; /billing's rows, its itemised bill, the header, checkout and sync all read it. Training eligibility and the invite email's seat kind now ignore removed roles too. (2) **three silent endings** — no key, no subscription id, already in step — each now logs one `[seat-sync]` line with the target and what Stripe charges, and the outcome is returned; a save posted from /billing lands back on /billing saying what happened to the subscription, and a refusal there says why on /billing instead of jumping to the chart. (3) **a leftover price override** — `STRIPE_PRICE_SEAT_MONTHLY` in Vercel would bill whatever price it names; on a live key `lib/pricing` now ignores an override that differs from its own id (logged), and /status names the setting (never its value) until it is deleted. Nothing re-syncs a subscription when a pricing RULE changes, so /billing now compares the live subscription with its own bill on load (one retrieve, cached a minute, streamed behind Suspense, 2.5s timeout, never blocking) and offers "Bring the subscription into line" when they differ. Tests: `tests/seat-sync-silent.test.ts`.
- 2026-09-25 — **Angus Shield** (the SPEC Business Solutions financial system) lives in its own private repo, `krisharold27-del/angus-shield`; its brief, rules and decisions are there. Nothing in SiteVIP changes: it stays one system in the financial-system connector category, never named here.
- 2026-09-25 — `/virtual-gm`, the whole business on one screen, is a door from My Page beside the Power Meter (shown to the same people the percentage is for), not a tab on the bar. It never scores anything itself: My Page and the Virtual GM both get their reading from `viewerPowerMeter` in `lib/power-meter-data` and draw it with the same `Dial` and `PowerBreakdown`, so the two cannot disagree. "Levers to pull this week" are only the slots the reading marks NOT MET (heavy hitters first), each naming the workflows `lib/workflows` says move that slot and the screen each starts on — an unmeasured slot is a KPI to set, not a lever. "Everything the business needs, one login" is the seven `FAMILIES`, counted from the map. "Your financial system" shows the business's own financials connection as it actually stands, and names Angus Shield plainly as SPEC's own financial system, not switchable yet. Logic in `lib/virtual-gm-overview`, tested in `tests/virtual-gm-overview.test.ts`.
- 2026-09-25 — **Claude recommends** is one engine (`lib/recommends`) for every decision SPEC helps with: a recommendation with its reason and the numbers behind it (one tap), "Ready to do this?" Yes / Not yet, and on Yes SPEC does exactly the action named and logs it in `decisions` with the figures it was based on. SPEC does the arithmetic; Claude only words it, in the background (`after()`), and `wordsHold` discards any wording that drops a figure, calls something ready that is not, or says "coming soon". The card says "Claude recommends" only when Claude worded it, otherwise "SPEC recommends". A Yes is re-derived on the server and must match the fingerprint shown (topic + action, never the daily counts, so Not yet is not re-asked every day); Not yet waits 14 days or until the recommendation changes. Too little data reads as exactly what is missing. First two topics: the labour rate (cost per hour ÷ billable share from 90 days of timesheets ÷ (1 − the 40% `MARGIN_BENCHMARK`), up to the next $5; SPEC holds no local-rate data and the card says so) and payroll readiness.
- 2026-09-25 — **Switch when ready** (`lib/switch`): day one changes nothing in a business's own systems. Six areas — accounting and payroll (Angus Shield), jobs, CRM, HR, safety (SPEC). A switch moves only the area's Coverage choices inside SPEC and snapshots them for Undo; the other system is never touched. The switch unlocks only when the owner has said "I'm ready" AND SPEC's check confirms (product able to run it + every data check met); owner-ready-but-unconfirmed shows "Not available yet" with exactly what is still being checked. Accounting starts in **Shadow** (Angus Shield beside the books; "No $10k–$15k setup cost. No migration project. Switch when you're ready."). Angus Shield cannot confirm today — it keeps no books, and SPEC holds no pay rates, pay cycles or leave balances — so payroll and accounting say "we'll tell you when it's ready" until `ANGUS_SHIELD.switchable` changes. **Simple Guarantee** ("if switching isn't easy, that month is free"): friction is logged per business with a fixed-list kind; /admin lists claims (business, month, area) so the month can be taken off the bill — crediting is done by hand in Stripe, not automated — and counts kinds across businesses; the business's own note never leaves it.
- 2026-09-25 — **Virtual GM + Virtual Admin**: SPEC runs both the GM and the admin department virtually. /virtual-gm now has the GM side (dial, levers, coverage, Claude recommends) and the Virtual Admin Department (payroll, invoicing, bills, compliance paperwork, HR admin, reporting — each on the screen that does it — plus the payroll and accounting cards). `VIRTUAL_GM.kicker`/`both` carry the positioning onto /spec and /pricing; the siteVIP landing says it too. "Virtual GM Power Meter" keeps its pinned name.
- 2026-09-25 — **The siteVIP front door (`/`) is the out-simple story.** Kris: hero "Simple." with *"Your GM and your admin department, run virtually."* and *"Don't pay three hundred grand for a GM. Just use SPEC."*; then the problem box (now on `/` as well as `/spec`, its reading blooming out piece by piece, reduced-motion respected); Virtual GM + Virtual Admin; Claude recommends (a labelled example, Yes / Not yet); Switch when ready with Angus Shield ("Stay in the black.") as the financial option; Make it simple, every week; the Simple Guarantee ("if it's not simple, that month is free"); a way back into the problem box; the Power Meter last. The seven-step job strip and the system toggles came off the page — they explained where this lets somebody try. **Honesty rule:** every feature section carries a flag in `lib/out-simple`; a flag may be on only when its evidence route exists (`tests/out-simple.test.ts`), and one that is off shows "Arriving now" with nothing to press. On at launch: Virtual GM, Claude recommends, Switch when ready, the Guarantee. Off: the weekly Make it simple page (not built). Angus Shield reads `ANGUS_SHIELD.switchable`, so the front door can never promise it before /switch can. The brief named two accounting products; the page says "your accounting system, your payroll" (the never list).
- 2026-09-25 — **Make it simple**, the weekly report the COGS meeting opens with (`lib/make-it-simple`, first item on /meeting): (1) what got simpler — decisions carried out, last week's accepted fixes done, and friction counts that fell since the last report; (2) the top three still complicated, from six counted signals (overdue invoices, pay runs with open award issues, open callbacks, enquiries over two days, timesheets waiting, actions carried three weeks), each filed under one of Kris's five kinds and shown as a live Claude recommends card — Yes needs an owner and becomes a meeting action tagged `simple:<key>`, which next week's report tracks to done; (3) what is ready to switch (the Switch when ready readings); (4) friction logged against the Simple Guarantee. SPEC had no meeting schedule, so `tenants.meeting_day`/`meeting_time` were added and asked once on /meeting; with none set the report runs weekly from Monday. "Automatically before the meeting" is done from inside each business: the report is written on the first visit from the day before the meeting (My Page and the Virtual GM ask after the page has gone; /meeting writes it on the spot if nobody has) — deliberately not a nightly job walking every business, which would be a cross-tenant read path. The person-or-process review stays off the shared report (it is GM-and-board only, `mayReadAutomationReview`); those two see a link to it. Reports are snapshots, one per business per meeting.
- 2026-09-25 — **The Angus Shield ⇄ SiteVIP contract** is `docs/ANGUS_SHIELD_SITEVIP_CONTRACT.md` (v1.0.0), identical in both repos and listed in CLAUDE.md's order of authority, so every SiteVIP session follows it. Kris: separate products (*"SiteVIP powered by SPEC"*, *"Angus Shield powered by SPEC"*, both by SPEC Business Solutions for SME businesses in any industry) that feel like one system to a customer using both. It fixes the one yes (SiteVIP's `financials` approval rule still applies), a master for every kind of data (SiteVIP: jobs, quotes, hours, crew, licences, purchase orders; Angus Shield: invoices, payments, bills, receipts, pay, leave balances, super, GST, job profit), signed events within 60 seconds plus a nightly catch-up, and what SiteVIP may never see (bank details, tax file numbers, payroll detail, the books). Consequences for SiteVIP, to build when the connection is built: a `job.completed` event when a job leaves `onsite`; `job_bills` become invoice requests while connected (Angus Shield holds the invoice of record); job margin shows Angus Shield's figure "from your books" while connected, SiteVIP's estimate otherwise. The earlier line that SiteVIP never names Angus Shield is superseded: it is a SPEC Business Solutions product and is named where the connection appears, as the Virtual GM page already does; outside vendors are still never named.

- 2026-09-25 — **Payroll is split** (Kris). SiteVIP's payroll job is the basics only — who worked, on which job, where and when: start/finish, breaks, travel, and allowance tags (site, overtime, meal; `ALLOWANCES` in `lib/timesheets`). SiteVIP never calculates tax, super or payslips, and holds no pay rate. All four steps are on **Jobs › Time**: the timesheet; **reconcile** (clashes and unfinished time hold entries back; gaps — booked with no time — time on a job not booked, time on a finished job, long days, no break, untagged weekend time and weeks over 38 hours are shown as "Is this correct?", never red); **approve** as a Claude recommends card that approves only what nothing is holding back; **send**, one tap, only once every entry in the week is approved — a file of hours (person, date, job, site, start, finish, break, travel, paid hours, allowances) any payroll system imports, recorded on `pay_runs` with `sent_to`. A pay week is Monday to Sunday (the Jobs week is Monday to Friday, and approving it had been leaving weekend hours behind). Fixed on the way: the old /people pay run was built from unapproved hours and its "send" produced no file — both actions are removed and People › Pay now points to Jobs › Time.
- 2026-09-25 — **Always free to choose** (Kris): every area keeps a full, equally simple path for the business's own system; Angus Shield is offered through Did you know? and Claude recommends with real reasons, never forced, nothing held back. Every switch card now says the own-system path beside the offer (`ownPath` in `lib/switch`), offers carry **Not yet** beside "I want this", and Not yet on an offer (`recheck: 'on_change'`) is respected until the recommendation itself changes — never on a timer; a dismissed card folds to one quiet line.
- 2026-09-25 — **Aligned to `docs/ANGUS_SHIELD_SITEVIP_CONTRACT.md` v1.0.0.** The seamless payroll path turns on only with a live Angus Shield connection (credential `provider = 'angus_shield'` on a live business `financials` connection, `angusConnected` in `lib/timesheets-data`) — not a SiteVIP toggle — and then approved hours go on approval (`hours.approved`) with no send step. Neither the connection nor the event sender exists in SiteVIP yet, so it is false for everyone and the export stands; whoever builds the connector must emit `hours.approved` from `approveCrewWeek` in the same change. **For Kris / both repos:** the contract's hours payload (§2) carries person, job, day, minutes, billable, approved by/at; the break, travel and allowance tags SiteVIP now records would be a 1.1 additive change, to be made in both repos the same day — not changed here.
- 2026-09-25 — **Financials is a key area on the bar, right after People** (`/financials`, `lib/financials`). Kris could not see the money: it was buried inside /virtual-gm. The page leads with cash in the bank, then five calm tiles (profit this month vs last, who owes you, who you owe, GST to pay the ATO, wages), a dot only when something needs attention, then What needs you, Payroll as three steps (Hours in → Checked → Sent to pay) and Your financial system. Figures come from the books first — a connected accounting system, else an uploaded CSV export (`ledger_uploads`, figures only, never the file) — and SiteVIP's own invoices and supplier bills fill who-owes-whom only when the books have not said. Nothing in yet shows a welcome with three ways in (Connect / Upload a file / Try Angus Shield), never a made-up figure. The financial-system panel is one component (`components/financial-system`) drawn on both /financials and /virtual-gm. The bar's ceiling moved from ten to eleven for it. Visible to anybody who manages and to the top of the chart; everyone else is told plainly on the page rather than redirected.
- 2026-09-25 — Site check decisions (Kris). The landing page's "$105/hr" labour-rate example stays: it is a business's own charge-out rate, never SPEC's price. Vendor names (Xero, MYOB, Simpro…) stay as quick-fill chips on the signed-in /connections and /setup/systems pages, where they help a person name the system they already run; public pages still name systems by category.
- 2026-09-25 — The Simple Guarantee is paid automatically. Telling SPEC something was not easy (/switch, on every area's page) credits one month of the business's subscription to its Stripe customer balance, which Stripe takes off the next invoice — a balance credit rather than a coupon, because it touches no invoice, subscription or price and can never be paid out. Once per business per month, three ways: `guarantee_credits` is unique on (tenant, month) and claimed before Stripe is asked; the create carries the idempotency key `simple-guarantee:<tenant>:<month>`; and the customer's balance transactions are read for one already tagged with that month. A business not paying yet is recorded as on us with nothing to credit. A Stripe failure is recorded, shown on /admin with Try again, and retried next time the business tells SPEC. The business is told "That month's on us — thanks for telling us." `lib/guarantee`, tested against the real Stripe library talking to a stand-in API; `scripts/guarantee-stripe-check.mts` runs it against a real Stripe TEST key and refuses a live one.
- 2026-09-25 — **Angus Shield is a separate product with one connection through the contract (Kris: "Separate product, one connection through the contract").** Supersedes the Design 19 line built this morning ("Angus Shield is a setting, not an integration": same login, same database, no keys). SiteVIP's end of the connection is built to `docs/ANGUS_SHIELD_SITEVIP_CONTRACT.md`: an administrator's one yes (board approval first, as for any financial system) sends them to Angus Shield with OAuth + PKCE (`/connections`, `lib/angus-shield-link`); the refresh token and the connection's signing secret are stored sealed (`secret-box`), and a refresh holds a row lock because a refresh token works once. Events out — `hours.approved` from `approveCrewWeek`, `job.won`/`job.completed`/`job.quoted`/`job.stage_changed` from `advanceJob`, `invoice.requested` when a variation is agreed or a claim/invoice is sent — go through `connection_outbox`, signed `X-SiteVIP-Signature`, sent straight after the response and retried by the five-minute `/api/ping` for a day. Events in at `/api/angus-shield/events` are checked against the connection's secret and applied once (`connection_inbox`): Angus Shield's invoice number lands on the job bill, `invoice.paid` marks the bill paid and moves an invoiced job to paid when all its bills are, `invoice.overdue` records days late (never red), `job.profit_updated` fills `job_book_figures` (the job's money "from your books"; newer wins). `lastSyncAt` is now written only when an event really crosses — never by a button or the sign-in — and `tests/no-false-feed.test.ts` holds that. The wording in `lib/angus.ts` is corrected to match, including payroll: SiteVIP captures and approves hours, Angus Shield processes the pay. Switches on when `ANGUS_SHIELD_CLIENT_SECRET` is set on this deployment.

- 2026-09-26 — **Kris: "the centrepiece is SPEC — the key products are Angus Shield and SiteVIP — the SPEC system is the power and the engine."** SPEC sits at the centre; SiteVIP (the operations side) and Angus Shield (the money side) are the two key products it powers, which is what "powered by SPEC" on each means. Separation is unchanged: each is its own product, codebase, database and login, joined only by the contract. Recorded in `CLAUDE.md` here and in angus-shield.
- 2026-09-26 — **Kris's method is a rule for every screen: the give-up-point law ("Where could someone give up here?"), the bar is love not tolerance, partial data is fine (an email or a phone, never both), and Link, Flow, Grow (nearly all the effort goes into surviving Link).** From his 2018 book *Mindfed* and ten years of practice across thirty-plus businesses, the field work SPEC itself came from. `docs/METHOD.md`, the same method as angus-shield's.
- 2026-09-26 — **Kris: "control is always in my page - this is associated with the administrator of the company."** Late quotes now sit on My Page for the business's administrator, as the 24 September design draws them: each quote whose chase is due, who and what, how late, which chase of three, SPEC's draft, "Approve SPEC's draft and send" and "Open it" (Jobs → Keep work coming). SPEC still sends nothing itself: the draft is copied into wherever the administrator already talks to that customer, and the button approves it and records it in `quote_chases`, the same row Jobs' "Sent it" writes, so the two screens never disagree and the same chase never goes twice. Nothing due, nothing shown. This clears the pinned-screen drift that had held CI red on main since the design landed.
